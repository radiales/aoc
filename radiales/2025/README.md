To start program put the input in the same directory and start it with the following commands

(if rust is not installed yet)
curl https://sh.rustup.rs -sSf | sh
source $HOME/.cargo/env

rustc d1t1.rs   //compile
./d1t1          //execute